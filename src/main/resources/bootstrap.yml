server:
  port: 15002
spring:
  profiles:
    active: local
    include: useDB
  output.ansi.enabled: 'ALWAYS'
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:15000